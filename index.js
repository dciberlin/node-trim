const {
    capitalizeInitial
} = require(`./formatting`);
const {
    showHelp
} = require(`./messaging`);

const str = process.argv.slice(2);
const help = str[0];


if(arg.includes(`--help`))
printHelp()
capitalizeInitial(str);
